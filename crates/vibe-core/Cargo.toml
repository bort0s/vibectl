[package]
name = "vibe-core"
description = "Core library for vibectl: manifest types, project detection, and the render engine."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
categories.workspace = true
keywords.workspace = true

[dependencies]
blake3 = { workspace = true }
directories = { workspace = true }
ignore = { workspace = true }
minijinja = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
toml_edit = { workspace = true }

[dev-dependencies]
insta = { workspace = true }
proptest = { workspace = true }
tempfile = { workspace = true }
# Negative control only — see the note in the workspace manifest. CI asserts
# this never reaches the non-dev dependency tree.
toml = { workspace = true }

[lints]
workspace = true
